import React from "react";
import { View, Text } from "react-native";

export default function EventsScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Events Screen (planned for later)</Text>
    </View>
  );
}
